import {Prisma} from "../../../generated/prisma/client";import {db} from "~/server/db";import type {NormalizedBillingEvent} from "./provider";
export async function persistBillingWebhook(provider:string,event:NormalizedBillingEvent){try{return await db.billingWebhookEvent.create({data:{provider,providerEventId:event.eventId,eventType:event.type,payload:event as unknown as Prisma.InputJsonValue}});}catch(error){if(error instanceof Prisma.PrismaClientKnownRequestError&&error.code==="P2002")return db.billingWebhookEvent.findUniqueOrThrow({where:{provider_providerEventId:{provider,providerEventId:event.eventId}}});throw error;}}
