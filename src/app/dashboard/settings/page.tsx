import {api} from "~/trpc/server";
export default function Settings(){async function requestDeletion(){"use server";await api.account.requestDeletion();}return <><h1 className="text-2xl font-bold">Settings</h1><p>Account preferences and deletion.</p><h2 className="mt-6 text-lg font-semibold">Test media upload</h2><form action="/api/media/upload" method="post" encType="multipart/form-data" className="mt-2 flex gap-2"><input type="file" name="file" accept="image/png,image/jpeg,image/webp,image/gif" required/><button className="border px-3 py-1">Upload</button></form><p className="text-sm">Uploads are signature-checked, private, and charged against your storage entitlement.</p><h2 className="mt-6 text-lg font-semibold">Delete account</h2><form action={requestDeletion}><button className="border border-red-700 p-2 text-red-700">Email deletion confirmation</button></form></>}
