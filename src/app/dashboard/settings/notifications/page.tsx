import { BellRing, Check } from "lucide-react";
import { getTranslations } from "next-intl/server";
export default async function NotificationsPage() { const t = await getTranslations("settings"); return <div className="space-y-6"><div><h3 className="text-lg font-semibold">{t("notifications")}</h3><p className="mt-1 text-sm text-muted-foreground">{t("notificationsDescription")}</p></div><div className="flex items-start gap-3 rounded-md border border-border p-4"><span className="flex size-9 shrink-0 items-center justify-center rounded-md bg-muted text-muted-foreground"><BellRing className="size-4" /></span><div className="flex-1"><p className="text-sm font-medium">{t("essentialEmails")}</p><p className="mt-1 text-sm text-muted-foreground">{t("essentialEmailsDescription")}</p></div><Check className="mt-1 size-4 text-success" /></div></div>; }
