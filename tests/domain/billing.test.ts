import {describe,expect,it} from "vitest";import {developmentBillingProvider} from "~/server/billing/provider";
describe("development billing signatures",()=>{it("rejects tampered signatures",()=>expect(developmentBillingProvider.verifyWebhook("payload","00")).toBe(false));it("produces a signed, normalized checkout event",async()=>{const value=await developmentBillingProvider.createCheckout("user","PREMIUM");const url=new URL(value.url);const encoded=url.searchParams.get("payload")!;expect(url.searchParams.get("signature")).toMatch(/^[a-f0-9]{64}$/);const event=developmentBillingProvider.normalizeWebhookEvent(Buffer.from(encoded,"base64url").toString("utf8"));expect(event).toMatchObject({userId:"user",planKey:"PREMIUM",status:"ACTIVE"});expect(event.eventId).toBeTruthy();expect(event.providerSubscriptionId).toBeTruthy();});});
